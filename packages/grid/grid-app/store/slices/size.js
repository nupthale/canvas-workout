export const initialState = {
    width: 0,
    height: 0,
    colWidths: {
        0: 100,
        1: 60,
        7: 60,
    },
    rowHeights: {
        3: 200,
    },
};

export const reducer = (state, action) => {
    switch(action.type) {
        case '':
            break;

        default:
            break;
    }
}

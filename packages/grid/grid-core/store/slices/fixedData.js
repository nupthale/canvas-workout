export const state = {
    fixedData: {
        fixedLeftRows: [],
        fixedRightRows: [],
        fixedHeaderRows: [],
        leftCornerRows: [],
    },
};

export const reducer = (state, action) => {
    switch(action.type) {
        case '':
            break;

        default:
            break;
    }
}

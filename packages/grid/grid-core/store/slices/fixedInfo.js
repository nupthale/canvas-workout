export const state = {
    fixedInfo: {
        fixedLeftWidth: 0,
        fixedHeaderHeight: 0,
    }
};

export const reducer = (state, action) => {
    switch(action.type) {
        case '':
            break;

        default:
            break;
    }
}
